// select all unordered list (ul) elements
const allunorderedList = document.querySelectorAll('ul')
console.log(allunorderedList)
// select all elements with the class "class-week"
const allClassweek=document.querySelectorAll(".class-week")
console.log(allClassweek)
// select all elements with the class "nav-bar"
const allNavbars = document.querySelectorAll(".nav-bar")
console.log(allNavbars)
// select the element with the id "dog-picture" and save it to a variable
const dogpicture = document.querySelectorAll("#dog-picture")
console.log(dogpicture)
